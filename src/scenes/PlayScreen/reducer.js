var initialState=[]
const playscreen = (state=initialState,action)=>{
    var index = -1
    var { id, product } = action
    switch(action.type){
        default: 
            return [...state]
    }
}

export default playscreen